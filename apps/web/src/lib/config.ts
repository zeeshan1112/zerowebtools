/**
 * AdSense Review Mode Toggle
 * Set this to `true` during the AdSense approval process.
 * This will:
 * 1. Temporarily exclude programmatic SEO, comparisons, recipes, and conversions from sitemap.xml.
 * 2. Add `noindex, nofollow` meta tags to all programmatic/doorway pages.
 * 
 * Flip this to `false` once Google AdSense approval is fully secured!
 */
export const ADSENSE_REVIEW_MODE = true;
